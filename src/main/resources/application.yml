spring:
  application:
    name: techabout
  profiles:
    include: secret

mybatis:
  mapper-locations: classpath:mappers/**/*.xml
  type-aliases-package: com.techabout.dto
  configuration:
    map-underscore-to-camel-case: true